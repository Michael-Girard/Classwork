TwoWayLinkedList

Creates a linked list that links both backwards and forwards (but isn't circular), fills it with random integers, then performs a series of tests to make sure the object performs correctly.
